# TradeFlow NG — Backend Dependencies
# Pinned for Python 3.11, tested on Cloud Run (linux/amd64)
# ─────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0        # includes uvloop + httptools
httptools==0.6.1
uvloop==0.19.0

# ── Auth & security ───────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# security.py imports bcrypt directly; pin it explicitly rather than relying
# on passlib's transitive dependency.
bcrypt>=4.0.0
python-multipart==0.0.9
cryptography>=42.0.0

# ── Database (async) ─────────────────────────────────────────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
# Sync driver for ML pipeline scripts that use psycopg2 directly
psycopg2-binary==2.9.9

# ── Data & ML ────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
# Prophet requires pystan — using the pre-built wheel
prophet==1.1.5
# PuLP for linear programming optimization
pulp==2.8.0

# ── HTTP client (for KoboToolbox API calls) ──────────────────
httpx==0.27.0
requests==2.31.0

# ── Settings & utilities ──────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Performance ───────────────────────────────────────────────
orjson==3.10.3        # faster JSON serialisation in FastAPI

starlette<0.41.0
